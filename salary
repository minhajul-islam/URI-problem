#include<stdio.h>
int main()
{
  int A,B;
  float C,D,result;
  scanf("%d",&A);
  printf("NUMBER = %d",A);
  scanf("%d %f",&B,&D);
  result=(B*C);
  printf("SALARY = U$ %f\n",result);



    return 0;

}
